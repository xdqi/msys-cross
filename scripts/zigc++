#!/bin/bash
# zig c++ wrapper — thin shim over the shared zig-driver (sccache routing + warning
# /flag filtering, adds -x c++ for compile units). See scripts/zig-driver.sh.
exec bash "$(dirname "$(readlink -f "$0")")/zig-driver.sh" c++ "$@"
